import React from 'react'

export default class Instructions extends React.Component {
  render() {
    return (
      <p>
        Are you unsure of whether to deposit money into a TFSA or a RRSP? Use this calculator.
        Adjust the values on the left, and the corresponding results will update on the right.
      </p>
    );
  }
}

